# 心石助手交互设计文档 v2

> 版本：MVP v2.0
> 日期：2026-03-28
> 核心原则：有限自主 - AI只控制养护计划和对话推荐

---

## 一、设计理念调整

### 1.1 AI权限边界

不是全权控制，而是**有限自主**：

| 区域 | AI权限 | 说明 |
|------|--------|------|
| 首页布局 | ❌ 固定 | 问候语、测评入口、任务列表位置固定 |
| 测评页面 | ❌ 固定 | 10题固定，选项固定 |
| **养护计划** | ✅ 完全自主 | AI选择模板+填充内容 |
| **对话推荐** | ✅ 完全自主 | AI推荐下一次提问建议 |
| 对话气泡 | ❌ 固定 | 样式固定，只填充内容 |

### 1.2 为什么这样设计

1. **降低复杂度**：不需要AI输出完整JSON结构
2. **保证稳定性**：核心页面不会因为AI输出异常而崩溃
3. **保留个性化**：养护计划是最需要个性化的部分，开放给AI
4. **增强引导**：对话推荐帮助用户知道下一步说什么

---

## 二、养护计划 - AI完全自主

### 2.1 养护计划模板库

前端提供6个模板，AI根据用户情况选择并填充内容：

```javascript
// 养护计划模板
const CARE_TEMPLATES = {
  // 模板1：晨曦观息（适合焦虑纠结型、高压心疲型）
  morning_breath: {
    id: 'morning_breath',
    icon: 'wb_sunny',
    title: '晨曦观息',
    timeHint: '清晨',
    colorClass: 'tertiary',
    descriptionTemplate: '{duration}分钟{action}。{rationale}',
    defaultAction: '呼吸引导',
    defaultRationale: '在清晨的第一缕光中，找回呼吸的节奏'
  },

  // 模板2：正念午后（适合认知耗竭型）
  mindful_afternoon: {
    id: 'mindful_afternoon',
    icon: 'energy_savings_leaf',
    title: '正念午后',
    timeHint: '午后',
    colorClass: 'primary',
    descriptionTemplate: '{action}。{rationale}',
    defaultAction: '放下手机，感受指尖的温度',
    defaultRationale: '在纷扰的午后为心灵留白'
  },

  // 模板3：修剪关系（适合社交消耗型）
  prune_relation: {
    id: 'prune_relation',
    icon: 'content_cut',
    title: '修剪关系',
    timeHint: '今天',
    colorClass: 'secondary',
    descriptionTemplate: '{action}。{rationale}',
    defaultAction: '主动推掉一个无效社交',
    defaultRationale: '给自己的时间表做个减法'
  },

  // 模板4：深夜复盘（适合所有类型）
  night_review: {
    id: 'night_review',
    icon: 'bedtime',
    title: '深夜复盘',
    timeHint: '睡前',
    colorClass: 'neutral',
    descriptionTemplate: '{action}。{rationale}',
    defaultAction: '写下今天搬开的三块石头',
    defaultRationale: '让负累留在今天，轻装入眠'
  },

  // 模板5：语言觉察（适合语言暗示型）
  language_watch: {
    id: 'language_watch',
    icon: 'record_voice_over',
    title: '语言觉察',
    timeHint: '全天',
    colorClass: 'primary',
    descriptionTemplate: '觉察{count}次负面表达，替换成「{replacement}」。{rationale}',
    defaultReplacement: '这件事有点棘手',
    defaultRationale: '语言暗示会直接影响身体状态'
  },

  // 模板6：五减默念（适合焦虑纠结型）
  five_reduce: {
    id: 'five_reduce',
    icon: 'self_improvement',
    title: '五减默念',
    timeHint: '睡前',
    colorClass: 'tertiary',
    descriptionTemplate: '花{duration}分钟默念五减法：{items}。{rationale}',
    defaultItems: '减去过度欲求、减去无谓焦虑、减去抱怨、减去对过去的纠结、减去对未来的恐惧',
    defaultRationale: '把心里的石头一块块搬出来'
  }
};
```

### 2.2 AI输出格式

AI只需要输出简单的结构：

```json
{
  "carePlan": {
    "selectedTemplates": ["morning_breath", "prune_relation"],
    "tasks": [
      {
        "templateId": "morning_breath",
        "customContent": {
          "duration": "5",
          "action": "呼吸引导",
          "rationale": "你昨天说睡不好，今天先从呼吸开始调整"
        }
      },
      {
        "templateId": "prune_relation",
        "customContent": {
          "action": "试着拒绝那个总来找你吐槽的同事",
          "rationale": "你在Day 3说同事吐槽让你累死了，这块石头今天试着搬开"
        }
      }
    ],
    "progressMessage": "已挪动两块心石，继续保持呼吸。",
    "recommendationReason": "根据你这几天的对话，你主要被社交消耗和睡眠问题困扰，这两个任务是最适合今天的。"
  }
}
```

### 2.3 前端渲染逻辑

```javascript
// 渲染养护计划
function renderCarePlan(aiResponse) {
  const { selectedTemplates, tasks, progressMessage } = aiResponse.carePlan;

  // 清空容器
  const container = document.getElementById('care-plan-container');
  container.innerHTML = '';

  // 渲染每个任务
  tasks.forEach(task => {
    const template = CARE_TEMPLATES[task.templateId];
    const card = createCareCard(template, task.customContent);
    container.appendChild(card);
  });

  // 渲染进度条和消息
  renderProgress(selectedTemplates.length, 4, progressMessage);
}
```

### 2.4 AI如何生成养护计划

AI的System Prompt中加入：

```markdown
## 养护计划生成规则

根据用户的内耗类型和对话历史，选择合适的养护模板并填充个性化内容。

### 模板选择规则

| 用户内耗类型 | 推荐模板 |
|--------------|----------|
| 语言暗示型 | language_watch, night_review |
| 社交消耗型 | prune_relation, night_review |
| 焦虑纠结型 | five_reduce, morning_breath |
| 高压心疲型 | morning_breath, night_review |
| 过度欲求型 | five_reduce, night_review |
| 认知耗竭型 | mindful_afternoon, night_review |

### 内容填充规则

1. **结合对话历史**：rationale必须引用用户之前说过的话
2. **符合减法理念**：每个任务不超过5分钟，不增加负担
3. **最多2个任务**：符合"一次只搬一块石头"的理念
4. **个性化action**：根据用户具体情况调整动作描述

### 示例

用户说："我每天加班到10点，同事总是来吐槽，我听完累死了"

AI选择：prune_relation, night_review
AI填充：
- prune_relation: action="试着拒绝那个同事的吐槽" rationale="你在对话里说同事吐槽让你累死了"
- night_review: action="睡前关手机10分钟" rationale="你说加班到10点，睡前给自己留点空间"

输出JSON格式。
```

---

## 三、对话推荐 - AI自主建议

### 3.1 AI输出格式

在对话响应中，AI可以附带推荐的下一步问题：

```json
{
  "message": "我特别懂这种「一句话卡一天」的感受。其实你这就是把别人的评价当成了石头，一直放在心里搬不开呀...",
  "suggestions": [
    "说说今天让你最纠结的事",
    "我总是担心别人的看法...",
    "这块石头好像一直在心里..."
  ],
  "highlightPhrase": "今天最让你纠结的一件事"
}
```

### 3.2 前端渲染

在对话气泡下方显示建议按钮：

```html
<!-- AI消息气泡 -->
<div class="ai-bubble">
  <p>我特别懂这种「一句话卡一天」的感受...</p>
</div>

<!-- AI推荐的问题建议 -->
<div class="suggestions-bar">
  <button class="suggestion-btn">说说今天让你最纠结的事</button>
  <button class="suggestion-btn">我总是担心别人的看法...</button>
  <button class="suggestion-btn">这块石头好像一直在心里...</button>
</div>
```

点击建议按钮 → 自动填充到输入框 → 用户可修改或直接发送

### 3.3 AI推荐规则

```markdown
## 对话推荐规则

每次回应后，生成2-3个用户可能想说的话作为建议。

### 推荐类型

1. **延续型**：继续当前话题的深入探讨
   - 用户说"领导批评我" → 推荐"那次批评具体说了什么？"

2. **关联型**：关联用户的其他内耗点
   - 用户是焦虑纠结型 → 推荐"你最近还有什么纠结的事？"

3. **行动型**：引导用户开始行动
   - 探讨完毕 → 推荐"今天试试做一个小动作"

### 推荐原则

1. 简短，不超过15字
2. 用用户的语言风格
3. 不要用"你应该"
4. 可以在正文中highlight某个短语，引导用户点击

### 示例

AI回应："现在，你可以跟我说说**今天最让你纠结的一件事**吗？"
suggestions: ["说说今天最让我纠结的事", "今天没有什么特别的纠结", "我想聊聊别的事"]
highlightPhrase: "今天最让你纠结的一件事"
```

---

## 四、完整交互流程

### 4.1 首页流程

```
用户打开首页
    │
    ▼
┌─────────────────────────────────────┐
│  固定UI：                            │
│  - 问候语（根据时间生成）             │
│  - 测评入口卡片                       │
│  - 今日减法任务列表（来自养护计划）    │
│  - 底部导航                          │
└─────────────────────────────────────┘
    │
    ▼
用户点击「开始测评」
    │
    ▼
进入测评页面（固定UI）
```

### 4.2 测评完成 → AI生成养护计划

```
用户完成10题测评
    │
    ▼
后端判定内耗类型
    │
    ▼
AI生成首次养护计划
    │
    │  AI输出：
    │  {
    │    "carePlan": {
    │      "selectedTemplates": ["morning_breath", "night_review"],
    │      "tasks": [...],
    │      "progressMessage": "今天先从这两块石头开始"
    │    }
    │  }
    │
    ▼
前端渲染养护计划卡片
    │
    ▼
显示在首页「今日减法」区域
```

### 4.3 对话流程（含推荐）

```
用户进入对话页面
    │
    ▼
┌─────────────────────────────────────┐
│  AI初始消息（基于测评结果）           │
│  + 建议按钮                          │
└─────────────────────────────────────┘
    │
    ▼
用户点击建议 或 自由输入
    │
    ▼
AI回应 + 新的建议
    │
    │  AI输出：
    │  {
    │    "message": "...",
    │    "suggestions": ["...", "...", "..."],
    │    "highlightPhrase": "..." // 可选
    │  }
    │
    ▼
前端渲染：
- 消息气泡（固定样式）
- 建议按钮（可点击）
- highlight短语（可点击）
```

### 4.4 对话结束 → 更新养护计划

```
用户结束对话（或每日结算）
    │
    ▼
AI根据本次对话内容更新养护计划
    │
    │  AI分析：
    │  - 用户今天聊了什么问题
    │  - 用户反馈了什么偏好
    │  - 用户完成了什么任务
    │
    │  AI输出新的carePlan
    │
    ▼
前端更新首页「今日减法」区域
```

---

## 五、技术实现

### 5.1 前端组件

```javascript
// 养护计划渲染器
class CarePlanRenderer {
  constructor() {
    this.templates = CARE_TEMPLATES;
  }

  // 渲染养护计划
  render(plan) {
    const container = document.getElementById('care-tasks');
    container.innerHTML = '';

    plan.tasks.forEach(task => {
      const card = this.renderCard(task);
      container.appendChild(card);
    });

    this.renderProgress(plan);
  }

  // 渲染单个卡片
  renderCard(task) {
    const template = this.templates[task.templateId];
    const content = task.customContent;

    // 使用模板生成描述
    const description = this.fillTemplate(template.descriptionTemplate, content);

    return createElement('div', {
      class: `care-card care-card--${template.colorClass}`,
      html: `
        <div class="care-card__icon">
          <span class="material-symbols-outlined">${template.icon}</span>
        </div>
        <h3 class="care-card__title">${template.title}</h3>
        <p class="care-card__desc">${description}</p>
        <button class="care-card__action">去执行</button>
      `
    });
  }

  // 填充模板
  fillTemplate(templateStr, content) {
    return templateStr.replace(/\{(\w+)\}/g, (match, key) => {
      return content[key] || '';
    });
  }
}

// 对话建议渲染器
class ChatSuggestionsRenderer {
  render(suggestions, highlightPhrase) {
    const container = document.getElementById('chat-suggestions');
    container.innerHTML = '';

    suggestions.forEach(text => {
      const btn = createElement('button', {
        class: 'suggestion-btn',
        text: text,
        onclick: () => this.fillInput(text)
      });
      container.appendChild(btn);
    });
  }

  fillInput(text) {
    document.getElementById('chat-input').value = text;
  }
}
```

### 5.2 后端API

```javascript
// OpenClaw Backend - 心石助手
import { OpenClawBackend } from './src/index.js';

const app = new OpenClawBackend({ port: 3000 });

// 1. 测评接口
app.tool('assess', async (params) => {
  const { userId, answers } = params;

  // 判定内耗类型
  const exhaustType = determineExhaustType(answers);

  // 保存用户画像
  await saveUserProfile(userId, { exhaustType, answers });

  // 生成初始养护计划
  const carePlan = await generateCarePlan(userId, exhaustType, null);

  return {
    exhaustType,
    report: generateReport(exhaustType),
    carePlan
  };
});

// 2. 对话接口（带记忆注入）
app.post('/chat', async (req, res, ctx) => {
  const { userId, message } = req.body;

  // 获取用户画像 + 最近对话
  const profile = await getUserProfile(userId);
  const history = await getRecentHistory(userId, 10);

  // 构建prompt（注入记忆）
  const prompt = buildPrompt(profile, history, message);

  // 调用AI
  const aiResponse = await ctx.invoke(userId, prompt);

  // 解析AI响应
  const parsed = parseAIResponse(aiResponse);

  // 保存对话
  await saveConversation(userId, message, parsed);

  // 返回
  res.json(parsed);
});

// 3. 养护计划更新接口
app.tool('update-care-plan', async (params) => {
  const { userId } = params;

  // 获取用户画像 + 最近对话
  const profile = await getUserProfile(userId);
  const history = await getRecentHistory(userId, 20);

  // AI生成新养护计划
  const carePlan = await generateCarePlan(userId, profile.exhaustType, history);

  // 保存
  await saveCarePlan(userId, carePlan);

  return carePlan;
});

app.start();
```

### 5.3 AI Prompt结构

```markdown
# 角色设定

你是「心石清理师」，遵循张海敏博士的减法养生理念...

## 养护计划生成

当需要生成养护计划时，输出以下JSON格式：

```json
{
  "carePlan": {
    "selectedTemplates": ["模板id1", "模板id2"],
    "tasks": [
      {
        "templateId": "模板id",
        "customContent": {
          "action": "个性化动作",
          "rationale": "为什么适合这个用户（引用对话）"
        }
      }
    ],
    "progressMessage": "进度提示语"
  }
}
```

## 对话推荐

每次回应后，输出推荐建议：

```json
{
  "message": "你的回应内容",
  "suggestions": ["建议1", "建议2", "建议3"],
  "highlightPhrase": "可选，正文中要高亮的短语"
}
```

## 记忆运用

- 引用用户之前说过的话
- 关联用户的内耗类型
- 对比用户的状态变化
```

---

## 六、总结

### 6.1 AI权限范围

| 权限 | 养护计划 | 对话推荐 |
|------|----------|----------|
| 选择模板 | ✅ | - |
| 填充内容 | ✅ | - |
| 生成建议 | - | ✅ |
| 高亮短语 | - | ✅ |

### 6.2 实现路径

1. **Phase 1**：实现养护计划模板库 + 前端渲染
2. **Phase 2**：实现对话建议渲染
3. **Phase 3**：实现AI Prompt + 后端接口
4. **Phase 4**：实现记忆注入 + 持久化

---

## 七、前端渲染要求

### 7.1 Markdown渲染支持

AI输出的消息内容支持Markdown格式，前端需要渲染：

| Markdown语法 | 渲染效果 | 用途 |
|--------------|----------|------|
| `**文本**` | 加粗 | 强调关键词、内耗类型 |
| `*文本*` 或 `_文本_` | 斜体 | 引用张博士的话 |
| `- 项目` | 无序列表 | 列出建议、任务 |
| `1. 项目` | 有序列表 | 步骤引导 |
| `> 引用` | 引用块 | 张海敏博士金句 |
| `[链接](url)` | 可点击链接 | 引导用户跳转 |

### 7.2 Markdown渲染实现

```javascript
// 使用 marked.js 或类似库
import { marked } from 'marked';

// 配置marked选项
marked.setOptions({
  breaks: true,        // 支持换行
  gfm: true,           // GitHub风格Markdown
  headerIds: false     // 不生成header id
});

// 渲染AI消息
function renderAIMessage(content) {
  const html = marked.parse(content);
  return `<div class="ai-bubble markdown-content">${html}</div>`;
}
```

### 7.3 Markdown样式

```css
/* Markdown内容样式 */
.markdown-content {
  line-height: 1.6;
}

.markdown-content strong {
  font-weight: 600;
  color: var(--primary);
}

.markdown-content em {
  font-style: italic;
  color: var(--secondary);
}

.markdown-content ul, .markdown-content ol {
  margin: 12px 0;
  padding-left: 20px;
}

.markdown-content li {
  margin: 6px 0;
}

.markdown-content blockquote {
  margin: 16px 0;
  padding: 12px 16px;
  background: var(--surface-container-low);
  border-left: 3px solid var(--tertiary);
  font-style: italic;
}

.markdown-content blockquote::before {
  content: "「";
  color: var(--tertiary);
}

.markdown-content blockquote::after {
  content: "」";
  color: var(--tertiary);
}
```

### 7.4 AI使用Markdown的示例

```json
{
  "message": "我看了你的测评结果，你属于**焦虑纠结型**。\n\n这种内耗往往来自于大脑不停地在模拟各种\"万一\"。\n\n> 张海敏博士说：90%的疾病，都是情绪拖出来的。\n\n现在你可以跟我说说今天最让你纠结的一件事吗？",
  "suggestions": ["说说今天最纠结的事", "我总是担心选错"]
}
```

渲染效果：

```
我看了你的测评结果，你属于 焦虑纠结型。

这种内耗往往来自于大脑不停地在模拟各种"万一"。

┌─────────────────────────────────────┐
│ 「张海敏博士说：90%的疾病，          │
│  都是情绪拖出来的。」                 │
└─────────────────────────────────────┘

现在你可以跟我说说今天最让你纠结的一件事吗？
```

---

> 文档结束