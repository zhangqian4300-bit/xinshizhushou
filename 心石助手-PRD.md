# 心石助手 PRD（产品需求文档）

> 版本：MVP v1.0
> 日期：2026-03-28
> 作者：张谦

---

## 一、产品概述

### 1.1 产品定位

**心石助手**是一个基于「减法养生」理念的情绪自愈AI助手，帮助上班族清理心里的"石头"——纠结、焦虑、别人的评价、消耗型关系。

**Slogan**：「做自己情绪的医生，每天5分钟，搬开心里的石头」

### 1.2 核心差异化

| 维度 | 传统情绪APP | 心石助手 |
|------|-------------|----------|
| 理念 | 加法养生（补品、功法） | 减法养生（清理心垃圾） |
| 内容 | 通用冥想/放松课程 | 个性化内耗分型匹配 |
| 交互 | 课程打卡、社群任务 | AI对话+每天1-2个落地小动作 |
| 记忆 | 无记忆，每次都是新对话 | 持久化记忆，AI越来越懂你 |
| 语气 | 专业术语、心理学黑话 | 像长辈聊天一样温和直白 |

### 1.3 目标用户

**核心人群**：情绪内耗的上班族

**典型画像**：
- 25-40岁，一二线城市
- 工作压力大，经常加班
- 容易纠结他人评价
- 有消耗型社交关系
- 睡眠质量不佳
- 总觉得"累"，但查不出身体问题

### 1.4 用户痛点

1. **情绪内耗无法自察**：不知道自己为什么累，找不到源头
2. **建议太空泛**：听过很多道理，但不知道今天具体该做什么
3. **AI对话没记忆**：每次聊天都像新认识，无法建立信任感
4. **心理咨询门槛高**：太贵、太专业、太正式

---

## 二、核心功能

### 2.1 功能架构

```
心石助手
├── 首页
│   ├── 理念介绍（简短）
│   ├── 测评入口
│   └── AI聊天入口
│
├── 测评系统
│   ├── 10题极简测评（1分钟）
│   ├── 内耗分型判定
│   └── 个性化诊断报告
│
├── AI情绪清理
│   ├── 实时对话（WebSocket）
│   ├── 持久化记忆
│   ├── 内耗类型匹配响应
│   └── 每日清理记录
│
├── 每日心气养护
│   ├── 个性化计划生成
│   └── 今日任务提醒（1-2个）
│
└── 个人中心
    ├── 我的测评报告
    ├── 我的清理历史
    └── 我的心路历程（可视化）
```

### 2.2 测评系统

#### 2.2.1 测评题目（10题版）

**引导语**：
> 我们每天都会往心里堆各种没用的石头：纠结、焦虑、别人的负面评价、消耗你的关系…
> 这套测评帮你快速找到最该先搬走的那一块，全程只需要10题，1分钟就能完成。

| 题号 | 维度 | 题目 | 选项 |
|------|------|------|------|
| 1 | 语言暗示 | 你是否经常把「烦死了」「累死了」「气死了」这类负面词挂在嘴边？ | A.一天好几次 / B.偶尔说 / C.几乎不说 |
| 2 | 独处空间 | 你每天能留出1小时完全不碰手机、不处理工作信息的无干扰时间吗？ | A.完全不行 / B.偶尔能 / C.每天都有 |
| 3 | 社交消耗 | 你身边有没有那种「见面聊完天，你感觉比干一天活还累」的人？ | A.有好几个 / B.有一两个 / C.几乎没有 |
| 4 | 焦虑纠结 | 你会不会反复纠结过去发生的糟心事，或总忍不住提前为还没发生的事焦虑？ | A.经常这样 / B.偶尔会 / C.很少 |
| 5 | 睡眠质量 | 你最近一个月，能轻松睡到自然醒，第二天起来感觉精力充足吗？ | A.很难 / B.偶尔可以 / C.基本都能 |
| 6 | 他人评价 | 你会不会因为别人一句负面评价，心里不舒服好几天？ | A.会 / B.偶尔会 / C.不会 |
| 7 | 信息输入 | 你每天的休闲时间，大部分都用来刷短视频/碎片化社交吗？ | A.是的 / B.一半一半 / C.经常读深度内容 |
| 8 | 欲求满足 | 你是不是总觉得"我还不够"，想要赚更多钱、买更好的东西？ | A.是的 / B.偶尔会焦虑 / C.不会 |
| 9 | 抱怨频率 | 你会不会经常和别人吐槽工作、生活里的不顺，挂在嘴边说个没完？ | A.每天都要 / B.偶尔吐槽 / C.很少抱怨 |
| 10 | 心流体验 | 你最近一个月，有没有过「投入一件事，完全忘记时间」的心流体验？ | A.从来没有 / B.偶尔有 / C.经常有 |

#### 2.2.2 内耗分型判定规则

| 内耗类型 | 判定规则 | 核心特征 |
|----------|----------|----------|
| 语言暗示型 | 题目1选A | 负面口头禅形成自我暗示 |
| 社交消耗型 | 题目3选A | 存在消耗型关系 |
| 焦虑纠结型 | 题目4选A 或 题目6选A | 反复纠结过去/他人评价 |
| 高压心疲型 | 题目2选A 且 题目5选A | 无独处时间+睡眠差 |
| 过度欲求型 | 题目8选A 且 题目9选A | 总觉得不够+抱怨多 |
| 认知耗竭型 | 题目7选A 且 题目10选A | 碎片化信息+无心流 |
| 复合型 | 同时满足2个及以上类型 | 多维度内耗叠加 |

#### 2.2.3 诊断报告模板

**通用结构**：
```
你的「心石头」诊断结果：{内耗类型}

你目前主要的情绪内耗来源：
{问题描述，贴合用户答题情况}

张海敏博士给你的专属减法建议：
1. {具体小动作1}
2. {具体小动作2}

给你的一句话提醒：
{理念金句}
```

**示例（焦虑纠结型）**：
> 你的「心石头」诊断结果：焦虑纠结型内耗
>
> 你目前主要的情绪内耗来源：
> 你总把过去的糟心事放在心里，或者忍不住提前为还没发生的事焦虑，这些情绪一直堆在心里，就变成了压住你的石头，慢慢就会影响身体状态。
>
> 张海敏博士给你的专属减法建议：
> 1. 今天就做一次「物理清理」：把让你纠结的事写在纸上，然后揉碎扔进垃圾桶，相当于把这块石头从心里搬出来
> 2. 每天花1分钟做「五减默念」："减去过度欲求，减去无谓焦虑，减去抱怨，减去对过去的纠结，减去对未来的恐惧"
>
> 给你的一句话提醒：
> 过去的已经过去，未来的还没来，你把心放在现在，石头搬开了，气血自然就顺了。

### 2.3 AI情绪清理

#### 2.3.1 AI角色设定（System Prompt）

```
# 角色定位
你是「心石清理师」AI助手，严格遵循北京协和医院张海敏博士提出的「减法养生」情绪自愈理念，为用户提供个性化的情绪清理服务。

# 必须严格遵守的回应规则

1. 所有输出必须贴合张海敏博士的核心观点：
   - 不得推荐任何名贵补品、复杂养生功法
   - 所有建议必须围绕"减去内耗、清理心垃圾"展开
   - 保持简洁，不增加用户负担

2. 语气要求：
   - 模仿临床医生温和直白的聊天口吻，像长辈聊天一样亲切
   - 不使用生硬的心理学黑话、专业术语
   - 直击痛点，给明确可落地的小步骤，不说空泛大道理

3. 个性化回应要求：
   必须根据用户的内耗类型，匹配对应的解决方案：
   - 【语言暗示型】：引导用户觉察替换负面口头禅
   - 【社交消耗型】：引导用户修剪消耗型关系，给出拒绝话术
   - 【焦虑纠结型】：引导用户做五减法
   - 【高压心疲型】：引导用户落实每日心气养护

4. 记忆运用：
   你拥有用户的完整对话历史和测评结果，每次回应都要体现出"我记得你"：
   - 引用用户之前说过的话/困扰
   - 关联用户的内耗类型
   - 对比用户之前的状态变化

# 固定回应框架

1. 先共情用户的情绪，不说"你应该"
2. 用张海敏的理念点出问题本质
3. 给出1-2个今天就能做的具体小动作

# 禁止事项

1. 严禁偏离张海敏减法养生理念
2. 严禁给用户开药方、做医学诊断
3. 严禁给用户增加负担，每次只给1-2个可落地小动作
```

#### 2.3.2 持久化记忆设计

**存储内容**：

| 数据类型 | 内容 | 用途 |
|----------|------|------|
| 用户画像 | 测评结果、内耗类型 | AI个性化匹配 |
| 会话历史 | 所有对话记录 | 上下文引用、状态对比 |
| 清理记录 | 每日清理的"心石头" | 可视化展示、成就感 |
| 个人计划 | 生成的每日养护计划 | 任务提醒 |

**记忆运用示例**：

用户第1天说：「今天领导批评我了，我纠结了一下午」

AI回应：「我特别懂这种"一句话卡一天"的感受，你这就是把别人的评价当成了石头…」

用户第7天说：「今天又遇到了类似的情况」

AI回应：「我记得上周你也遇到过领导批评的事，当时你说纠结了一下午。这次你感觉怎么样？是不是比上次好一点了？」

**技术方案**：
- 用户ID作为主键（小程序用openid，APP用设备ID+账号）
- 会话记录按时间戳存储，支持按日期检索
- 每次AI对话，自动注入：用户画像 + 最近10条会话摘要 + 当前日期的清理记录

### 2.4 每日心气养护计划

#### 2.4.1 个性化计划生成逻辑

根据用户的内耗类型和作息情况，生成每日计划：

| 内耗类型 | 推荐任务 |
|----------|----------|
| 语言暗示型 | 今日觉察3次负面表达，替换成平和说法 |
| 社交消耗型 | 本周主动推掉1个无效社交 |
| 焦虑纠结型 | 每天花1分钟做「五减默念」 |
| 高压心疲型 | 每天留15分钟无干扰独处时间 |
| 过度欲求型 | 写下3个你最近强行想要的目标，划掉1个非必需的 |
| 认知耗竭型 | 每天抽15分钟读一页"费脑子"的书 |

**适配规则**：
- 如果用户是通勤忙的上班族 → 把任务拆成小块（早15分钟+晚45分钟）
- 如果用户是自由职业 → 推荐完整固定时段
- 任务数量严格控制：最多2个，符合减法理念

### 2.5 个人中心

#### 2.5.1 我的清理历史

**展示内容**：
- 按日期展示清理记录
- 每条记录显示：清理的"心石头"类型、AI给出的建议、用户反馈

**可视化**：
- 「心石清理进度」图表：累计清理X块石头
- 「内耗变化曲线」：基于后续对话的情绪变化趋势

#### 2.5.2 我的测评报告

- 显示最新的测评结果
- 支持重新测评
- 显示历史测评对比（如果有多次测评）

---

## 三、技术方案

### 3.1 技术架构

```
┌─────────────────────────────────────────────────────────────┐
│                      前端层                                  │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐          │
│  │   小程序    │  │  独立APP   │  │  管理后台   │          │
│  └─────────────┘  └─────────────┘  └─────────────┘          │
└─────────────────────────────────────────────────────────────┘
                          │ HTTP/WebSocket
                          ▼
┌─────────────────────────────────────────────────────────────┐
│                   OpenClaw Backend                           │
│  ┌─────────────────────────────────────────────────────┐    │
│  │  Express HTTP Server                                 │    │
│  │  ├── /chat          → AI对话                         │    │
│  │  ├── /assess        → 测评系统                        │    │
│  │  ├── /report        → 诊断报告                        │    │
│  │  ├── /plan          → 每日计划                        │    │
│  │  ├── /history       → 清理历史                        │    │
│  │  └── /ws            → WebSocket实时对话               │    │
│  └─────────────────────────────────────────────────────┘    │
│  ┌─────────────────────────────────────────────────────┐    │
│  │  持久化存储                                          │    │
│  │  ├── 用户画像（内耗类型、测评结果）                   │    │
│  │  ├── 会话历史（对话记录）                             │    │
│  │  ├── 清理记录（每日心石头）                           │    │
│  │  └── 个人计划（每日养护任务）                         │    │
│  └─────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────┘
                          │ WebSocket
                          ▼
┌─────────────────────────────────────────────────────────────┐
│                   OpenClaw Gateway                           │
│  ┌─────────────────────────────────────────────────────┐    │
│  │  AI Agent                                            │    │
│  │  ├── 接收消息 + System Prompt                        │    │
│  │  ├── 调用模型生成响应                                │    │
│  │  ├── 流式输出返回                                    │    │
│  └─────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────┘
```

### 3.2 OpenClaw Backend 集成方案

基于现有的 `OpenClawBackend` 框架，注册以下接口：

```javascript
// 心石助手 Backend
import { OpenClawBackend } from 'openclaw-backend-framework';

const app = new OpenClawBackend({ port: 3000 });

// 1. 注册测评工具
app.tool('assess', async (params) => {
  // params: { answers: [A, B, A, ...] }
  // 返回：内耗类型 + 诊断报告
  return assessHandler(params);
});

// 2. 注册AI对话（带记忆注入）
app.post('/chat', async (req, res, ctx) => {
  const userId = req.body.userId;
  const message = req.body.message;

  // 获取用户画像 + 会话历史
  const userProfile = await getUserProfile(userId);
  const recentHistory = await getRecentHistory(userId, 10);

  // 构建带记忆的prompt
  const enrichedMessage = buildMemoryPrompt(userProfile, recentHistory, message);

  // 调用 OpenClaw Gateway
  const result = await ctx.invoke(userId, enrichedMessage);
  res.json(result);

  // 存储本次对话
  await saveConversation(userId, message, result);
});

// 3. WebSocket 实时对话
app.post('/ws', async (req, res, ctx) => {
  // 流式对话接口
});

// 4. 清理历史查询
app.tool('history', async (params) => {
  const userId = params.userId;
  return getClearHistory(userId);
});

// 5. 每日计划生成
app.tool('plan', async (params) => {
  const userId = params.userId;
  const userProfile = await getUserProfile(userId);
  return generateDailyPlan(userProfile);
});

app.start();
```

### 3.3 持久化存储方案

**方案选择**：SQLite（轻量）或 PostgreSQL（扩展性）

**数据模型**：

```sql
-- 用户画像表
CREATE TABLE user_profiles (
  user_id TEXT PRIMARY KEY,
  assess_result TEXT,       -- JSON: 测评结果
  exhaust_type TEXT,        -- 内耗类型
  created_at TIMESTAMP,
  updated_at TIMESTAMP
);

-- 会话历史表
CREATE TABLE conversations (
  id SERIAL PRIMARY KEY,
  user_id TEXT,
  role TEXT,                -- 'user' or 'assistant'
  content TEXT,
  created_at TIMESTAMP
);

-- 清理记录表
CREATE TABLE clear_records (
  id SERIAL PRIMARY KEY,
  user_id TEXT,
  date DATE,
  stone_type TEXT,          -- 清理的心石头类型
  content TEXT,             -- 用户描述
  suggestion TEXT,          -- AI建议
  created_at TIMESTAMP
);

-- 每日计划表
CREATE TABLE daily_plans (
  id SERIAL PRIMARY KEY,
  user_id TEXT,
  date DATE,
  tasks TEXT,               -- JSON: 任务列表
  completed BOOLEAN,
  created_at TIMESTAMP
);
```

### 3.4 记忆注入机制

每次AI对话前，自动构建记忆prompt：

```javascript
function buildMemoryPrompt(userProfile, recentHistory, currentMessage) {
  const exhaustTypeInfo = EXHAUST_TYPE_MAP[userProfile.exhaustType];

  return `
【用户画像】
- 内耗类型：${userProfile.exhaustType}
- 测评结果摘要：${summarizeAssess(userProfile.assessResult)}

【近期对话摘要】
${summarizeHistory(recentHistory)}

【当前日期】
${new Date().toLocaleDateString('zh-CN')}

【用户当前消息】
${currentMessage}

请根据用户画像和对话历史，以「心石清理师」的身份回应。体现出你记得用户的情况，回应要贴合其内耗类型。
  `;
}
```

---

## 四、产品形态演进

### 4.1 阶段规划

| 阶段 | 产品形态 | 核心功能 | 目标 |
|------|----------|----------|------|
| Phase 1 | 小程序 | 测评 + AI对话 + 清理历史 | MVP验证需求 |
| Phase 2 | 小程序迭代 | 每日计划 + 可视化 | 增强留存 |
| Phase 3 | 独立APP | 全功能 + 更多个性化 | 用户沉淀 |
| Phase 4 | SaaS | 多用户 + 企业版 | 商业化 |

### 4.2 MVP优先级

**必须有**：
1. 10题测评 + 分型判定
2. AI对话（带记忆注入）
3. 清理历史查看

**可以有**：
1. 每日心气养护计划
2. 心石清理进度可视化

**暂不做**：
1. 社群功能
2. 复杂打卡系统
3. 付费会员体系

---

## 五、附录

### 5.1 六种内耗类型专属建议汇总

| 内耗类型 | 核心问题 | 专属建议 |
|----------|----------|----------|
| 语言暗示型 | 负面口头禅形成自我暗示 | 觉察替换负面表达 |
| 社交消耗型 | 存在消耗型关系 | 修剪关系，给出拒绝话术 |
| 焦虑纠结型 | 反复纠结过去/他人评价 | 五减法默念 |
| 高压心疲型 | 无独处时间+睡眠差 | 每日心气养护 |
| 过度欲求型 | 总觉得不够+抱怨多 | 欲望清理 |
| 认知耗竭型 | 碎片化信息+无心流 | 深度阅读替代短视频 |

### 5.2 UI文案模板

**首页banner**：
> 你好，我是你的「心石清理师」
>
> 我们每天都在往心里堆石头：别人的评价、无解的焦虑、消耗你的关系、说不完的抱怨…
> 这些石头压得你累，慢慢就会影响身体。
>
> 北京协和张海敏博士说：最高级的养生，是给心里做减法，搬开石头，你自己就是最好的医生。

**测评入口按钮**：
> 1分钟测一测，你心里有几块石头？

**AI聊天顶栏**：
> 你可以直接把今天的不开心、烦心事说给我，我会按照张海敏博士的减法理念帮你清理。

**个人中心一句话**：
> 搬一块，少一块，慢慢来，就是最好的养生。

---

> 文档结束